@model SHOPTHL.Data.Thuonghieu

@{
    ViewData["Title"] = "Edit";
    Layout = "_Admin";
}

<h1>Chỉnh sửa</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Math" />
            <div class="form-group">
                <label asp-for="Tenth" class="control-label">Tên thương hiệu</label>
                <input asp-for="Tenth" class="form-control" />
                <span asp-validation-for="Tenth" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Lưu" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-info">Trở về trang danh sách</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
